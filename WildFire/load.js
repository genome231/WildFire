let merits = 200;
registerVariable("merits","Merits","Merits determine how popular you are with the Domme.");
//Yet again. merits = 200 only creates a variable in the code but not for TAJ. If you want to store it throughout multiple sessions use this:
setVar("merits", 200);
let strokePace = 60;
